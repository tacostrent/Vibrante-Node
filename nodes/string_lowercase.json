{
    "node_id": "string_lowercase",
    "name": "Lowercase",
    "description": "Converts text to lowercase.",
    "category": "String",
    "icon_path": "icons/uppercase.svg",
    "inputs": [
        { "name": "exec_in", "type": "exec" },
        { "name": "text", "type": "string" }
    ],
    "outputs": [
        { "name": "exec_out", "type": "exec" },
        { "name": "result", "type": "string" }
    ],
    "python_code": "from src.nodes.base import BaseNode\n\nasync def execute(self, inputs):\n    text = inputs.get('text') or self.get_parameter('text', '')\n    try:\n        res = str(text).lower()\n    except Exception as e:\n        self.log_info(f'Lowercase error: {e}')\n        res = ''\n\n    await self.set_output('result', res)\n    await self.set_output('exec_out', True)\n    return {'result': res}"
}
