{
    "node_id": "hou_mcp_environment_review",
    "name": "hou_mcp_environment_review",
    "description": "6-dimension environment quality review: structure (0.35), zones (0.15), anchors (0.25), support (0.10), decoration (0.10), atmosphere (0.05). production_ready requires score >= 0.70 and no blocking findings.",
    "category": "Houdini",
    "icon_path": "icons/houdini.svg",
    "use_exec": true,
    "inputs": [
        {"name": "environment_name", "type": "string", "widget_type": "text", "options": null, "default": ""},
        {"name": "structure_json",   "type": "string", "widget_type": "text", "options": null, "default": ""},
        {"name": "zones_json",       "type": "string", "widget_type": "text", "options": null, "default": ""},
        {"name": "anchor_json",      "type": "string", "widget_type": "text", "options": null, "default": ""},
        {"name": "support_json",     "type": "string", "widget_type": "text", "options": null, "default": ""},
        {"name": "decoration_json",  "type": "string", "widget_type": "text", "options": null, "default": ""},
        {"name": "atmosphere_json",  "type": "string", "widget_type": "text", "options": null, "default": ""}
    ],
    "outputs": [
        {"name": "review_json",      "type": "string", "widget_type": null, "options": null, "default": null},
        {"name": "overall_score",    "type": "float",  "widget_type": null, "options": null, "default": null},
        {"name": "grade",            "type": "string", "widget_type": null, "options": null, "default": null},
        {"name": "production_ready", "type": "bool",   "widget_type": null, "options": null, "default": null}
    ],
    "python_code": "from src.nodes.base import BaseNode\nfrom src.runtime.environment.environment_review import get_environment_review\nfrom src.runtime.environment.environment_structure_builder import EnvironmentStructurePlan\nfrom src.runtime.environment.environment_zone_builder import ZonePlan\nfrom src.runtime.environment.anchor_asset_builder import AnchorPlan\nfrom src.runtime.environment.support_asset_builder import SupportPlan\nfrom src.runtime.environment.decorative_population import DecorationPlan\nfrom src.runtime.environment.atmosphere_builder import AtmospherePlan\nimport json\n\nclass Hou_Mcp_Environment_Review(BaseNode):\n    name = \"hou_mcp_environment_review\"\n\n    def __init__(self):\n        super().__init__()\n        # [AUTO-GENERATED-PORTS-START]\n        self.add_input(\"environment_name\", \"string\", widget_type=\"text\", default=\"\")\n        self.add_input(\"structure_json\",   \"string\", widget_type=\"text\", default=\"\")\n        self.add_input(\"zones_json\",       \"string\", widget_type=\"text\", default=\"\")\n        self.add_input(\"anchor_json\",      \"string\", widget_type=\"text\", default=\"\")\n        self.add_input(\"support_json\",     \"string\", widget_type=\"text\", default=\"\")\n        self.add_input(\"decoration_json\",  \"string\", widget_type=\"text\", default=\"\")\n        self.add_input(\"atmosphere_json\",  \"string\", widget_type=\"text\", default=\"\")\n        self.add_output(\"review_json\",      \"string\")\n        self.add_output(\"overall_score\",    \"float\")\n        self.add_output(\"grade\",            \"string\")\n        self.add_output(\"production_ready\", \"bool\")\n        # [AUTO-GENERATED-PORTS-END]\n\n    def _parse(self, text, cls):\n        if not (text or \"\").strip():\n            return None\n        try:\n            return cls.from_dict(json.loads(text))\n        except Exception:\n            return None\n\n    async def execute(self, inputs):\n        env_name = (inputs.get(\"environment_name\") or \"\").strip()\n\n        try:\n            structure_plan  = self._parse(inputs.get(\"structure_json\"),  EnvironmentStructurePlan)\n            zone_plan       = self._parse(inputs.get(\"zones_json\"),       ZonePlan)\n            anchor_plan     = self._parse(inputs.get(\"anchor_json\"),      AnchorPlan)\n            support_plan    = self._parse(inputs.get(\"support_json\"),     SupportPlan)\n            decoration_plan = self._parse(inputs.get(\"decoration_json\"),  DecorationPlan)\n            atmosphere_plan = self._parse(inputs.get(\"atmosphere_json\"),  AtmospherePlan)\n\n            reviewer = get_environment_review()\n            result = reviewer.review(\n                env_name,\n                structure_plan=structure_plan,\n                zone_plan=zone_plan,\n                anchor_plan=anchor_plan,\n                support_plan=support_plan,\n                decoration_plan=decoration_plan,\n                atmosphere_plan=atmosphere_plan,\n            )\n\n            prod_str = \"YES\" if result.production_ready else \"NO\"\n            self.log_info(f\"Review '{env_name}': score={result.overall_score:.2f}, grade={result.grade}, production={prod_str}\")\n            if result.findings:\n                for f in result.findings:\n                    self.log_error(f\"Finding: {f}\")\n\n            return {\n                \"review_json\":      json.dumps(result.to_dict(), sort_keys=True, default=str),\n                \"overall_score\":    result.overall_score,\n                \"grade\":            result.grade,\n                \"production_ready\": result.production_ready,\n                \"exec_out\": True,\n            }\n        except Exception as exc:\n            self.log_error(f\"Environment review failed: {exc}\")\n            return {\"review_json\": \"{}\", \"overall_score\": 0.0, \"grade\": \"F\", \"production_ready\": False, \"exec_out\": True}\n\ndef register_node():\n    return Hou_Mcp_Environment_Review\n"
}
